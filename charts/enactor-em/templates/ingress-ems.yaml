{{- if .Values.ingress.ems.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ems.name" . }}
  labels:
    {{- include "ems.labels" . | nindent 4 }}
  {{- with .Values.ingress.ems.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          {{- range .Values.ingress.ems.paths }}
          - path: {{ . }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "ems.name" $ }}
                port:
                  name: http
          {{- end }}
{{- end }}
